{{#each week as |day index|}}
    <div>
        <h2 class="header {{index}}">{{day}}</h2>
        <ul class="day {{index}}">
            {{calendar-item day=day projects=model}}
        </ul>
    </div>
{{/each}}


